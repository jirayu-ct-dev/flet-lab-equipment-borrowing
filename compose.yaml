services:
  app:
    build: .
    image: lab-equipment-borrowing:latest
    container_name: lab-equipment-borrowing
    ports:
      - "8080:8080"
    environment:
      APP_DB_PATH: /app/data/lab_equipment.db
    volumes:
      - ./data:/app/data
